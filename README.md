# Visual Studio Code Path Comment

Adds a comment with the file's relative path to the top of each file on save, using the same format as ChatGPT when displaying code file paths.

For example:
```ts
// src/app/data/route.ts
```